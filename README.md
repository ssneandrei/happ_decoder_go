# Happ Decoder Go

Микро-сервис для локальной расшифровки VPN-подписок формата `happ://` (crypt – crypt5) на роутерах с OpenWrt.

## Быстрый запуск

### Запуск на роутере
```bash
./happ-decoder-linux-arm64 -port 8080
